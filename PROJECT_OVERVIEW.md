# Willy 项目总览：面向可信分子模拟的 Agent 工作流

> 当前版本：`0.3.1`。本版本支持本机 MD 执行和用户自配 OpenAI-compatible LLM；远程执行与托管网关已从产品入口移除并冻结为后续版本参考。本版完善了 OpenAI-compatible 工具调用兼容、配置审计错误提示和本机依赖预检展示。

> 定位：Willy 将自然语言需求转化为可审计的分子动力学工作流。LLM 不直接执行科学计算，而是在受限工具、结构化配置、确定性编排和人工授权的边界内参与决策。
>
> 本文件用于项目介绍、答辩和交接；具体接口与领域细节以 `docs/` 下的设计文档为准。本文件不纳入文档台账。

## 1. 问题与设计选择

一个可用的 MD 任务并不等价于一次 GROMACS 命令。它需要跨越体系描述、量子输入、RESP 电荷、力场参数、拓扑、建盒、平衡、生产和结果分析，而且每一步都依赖上一层真实产物。

```text
自然语言体系描述
  -> 分子识别、数量/电荷/温度/时长解释
  -> 可验证的 config.json
  -> 量子结构与 RESP 电荷
  -> 分子拓扑与主拓扑
  -> 基于质量密度的 Packmol 初始盒子
  -> GROMACS EM -> EQ -> PROD
  -> 状态、产物、错误、受控恢复与内部实验后处理
```

Willy 的核心选择是：**LLM 负责语言理解、诊断候选和用户沟通；确定性代码负责文件、状态、策略、权限、外部命令和科学计算。** 这避免了传统脚本的人工传递成本，也避免了聊天模型以不受约束的语言生成替代真实工作流。

## 2. 设计原则

1. **LLM 是受限决策者。** Agent 只能调用本层公开的 function-calling 工具，不拥有 shell、任意写文件或跨层工具权限。
2. **事实与解释分离。** 配置、状态、产物、ETA 和软件能力由注册表及 manifest 提供；LLM 只能解释这些事实，不能补造结果。
3. **产物先于流程。** 文件存在不等于阶段完成；下一步只能消费已登记、已校验且获许可的上游产物。
4. **高影响动作可审计。** 配置修改、重跑、派生运行和停止有显式影响等级、策略裁决和用户确认边界。
5. **运行隔离。** 每个任务在 `md_run/<run_id>/` 中独立执行，避免共享目录、旧产物与并发任务污染。
6. **公开信息最小化。** 前端只展示文件/操作级状态、脱敏错误摘要和登记产物，不泄露命令、绝对路径、原始日志、Prompt 或密钥。
7. **证据优先。** mock、预检、真实外部执行和科学收敛是不同层级的证据，不能互相替代。

## 3. 真实执行链路

```text
Gradio 前端
  -> Config Agent
  -> config.json 结构校验与用户确认
  -> Pipeline Launch 预占启动锁、分配 run_id
  -> md_run/<run_id>/config.json 快照与 provenance
  -> PipelineOrchestrator 按注册表执行 10 步
  -> RunRegistry 对前端和运行助理发布受限事实
```

启动前会验证配置外形、MD 协议版本、后端组合、分子可用性和量子输入。启动锁将浏览器会话、子进程与 `run_id` 绑定，避免重复点击启动两个相互覆盖的任务。

| 步骤 | 名称 | 后端 | 核心产物或许可 |
|---:|---|---|---|
| 1 | 结构优化 | Gaussian 16 / ORCA | 已优化量子结构 |
| 2 | 单点计算与 mol2 转换 | G16/formchk / ORCA/orca_2mkl | 可消费 `.mol2` |
| 3 | RESP 电荷计算 | Multiwfn | `.chg` |
| 4 | 分子拓扑参数化 | Sobtop / LigParGen+BOSS | `.itp`、`.gro`、组件登记 |
| 5 | 主拓扑生成 | 内部组装器 | `topol.top` 与修订 ITP |
| 6 | MDP 参数生成 | 内部协议生成器 | EM/EQ/PROD MDP bundle |
| 7 | Packmol 建盒 | Packmol + GROMACS 预处理 | PDB、盒矢量、初始密度审计 |
| 8 | 能量最小化 | GROMACS | 通过许可的 EM 产物 |
| 9 | 三点式退火平衡 | GROMACS | 通过许可的 EQ 产物与 checkpoint |
| 10 | 生产模拟 | GROMACS | PROD 产物与 checkpoint |

模拟阶段严格为 `EM -> EQ -> PROD`。默认 EQ 是高温、过渡温度和目标温度组成的三点式退火，含六段可定义时长；初始体积默认按 `0.7 g/cm3` 估算，实际盒矢量、体积和质量密度在 run 内记录。PROD 必须消费通过验收的 EQ 许可与匹配 checkpoint，不能仅以 `eq.gro` 的存在作为依据。

已实现 Sobtop 的 GAFF/UFF 和 LigParGen/BOSS 的 OPLS-AA 两条独立拓扑路径；它们不可在同一 run 中混用。2026-08-11 的四 profile 真实验收中，G16/ORCA + Sobtop/LigParGen 均完成 10/10；中性 OPLS 组合的单一 Ewald 净电荷 warning 按 `0.15e` 受控容差放行并已回归。AMBER、离子 OPLS 参数化和其他力场混用尚未作为公开能力。

## 4. 文字到配置：轻量 RAG 与结构化校验

### 4.1 配置不是自由文本

Config Agent 将输入路由为“单次查询/操作”或“新体系配置”。配置模式遵循固定顺序：

```text
提取组分名称
  -> 查询每个组分
  -> 必要时刷新 struct/ 与知识库
  -> 化合物拆分、别名归一和电荷检查
  -> 读取 MD 默认值
  -> 输出固定 JSON 外形
  -> schema 与业务规则二次验证
```

模型只生成候选 `config.json`，不是直接启动指令。无效组分、歧义名称和非法数值会阻塞；净电荷不平衡和过大体系是对用户可见的非阻塞警告。

### 4.2 面向分子实体识别的轻量 RAG

`_MoleculeRegistry` 解析 `docs/knowledge.md` 的分子表，并结合 `struct/` 中真实可用的输入。它使用字符边界 n-gram 的 TF-IDF 向量化和余弦相似度，为中文别名、英文名称、离子写法与轻微拼写差异做近似匹配：

```text
用户文本 -> TF-IDF 字符向量 -> 候选分子/别名匹配
         -> tools_lookup_molecule 的结构化事实
         -> LLM 形成候选配置 -> 代码校验
```

这是一个**面向实体识别的轻量检索增强生成（RAG）**，不是通用问答的密集向量数据库。检索通过工具返回电荷、自旋、基组、别名和可用性，之后再由 schema、净电荷规则和输入文件检查收束。因此“模型知道某个分子”不会被误认为“当前机器能够计算该分子”。

## 5. 分层 Agent 与 Prompt 结构

| 层 | Agent | 职责 | 权限边界 |
|---|---|---|---|
| 0 | Config Agent | 需求理解、配置候选、方案说明 | 配置工具；启动需用户确认 |
| 1 | Quantum Agent | 量子优化、转换、RESP 失败诊断 | 量子层工具 |
| 2 | Topology Agent | 力场、参数化、主拓扑和 ITP 问题 | 拓扑层工具 |
| 3 | Simulation Agent | MDP、建盒、EM/EQ/PROD 诊断与重跑提案 | 模拟层工具；高影响修改需确认 |
| 运行层 | Run Assistant | 当前 run 的状态、产物、ETA 和错误摘要 | 严格只读 |

这种架构采用最小权限、领域工具隔离和分层错误归属。发生问题时，拥有真实输入、产物与工具上下文的层负责形成候选处理，而不是由通用模型跨层猜测。

Prompt 不是唯一安全边界，而是模型的第一层行为约束；动作契约、恢复策略和状态机才是最终裁决者。

**Config Prompt** 包含模式路由、固定 JSON 模板、逐分子查询顺序、别名/数量/密度/力场规则，以及正常、ORCA、歧义和高算力警告的少样本例子。

**领域修复 Prompt** 统一包含角色范围、工具白名单、输入事实、禁止行为、错误分类、修复候选和升级条件。Layer Agent 通过 function-calling 循环执行，并受轮次、时间和费用预算约束。工具、模型或参数错误都会转为结构化结果或升级，不会直接穿透到前端。

**运行助理 Prompt** 强制只读，只能依据当前 run 的公开工具结果回答；它不得把 ETA 说成保证，不得展示日志、命令、路径或密钥，也不能把“暂停”等聊天文本理解为停止命令。停止由独立按钮和服务端二次确认完成。

模型标识和 Prompt 版本会被写入决策记录，允许比较同一失败在不同模型或 Prompt 版本下的处理差异。

## 6. 标准动作契约与恢复策略

Agent 可见工具通过标准动作模型表达，而不是只依赖 Prompt 中“请谨慎”的自然语言：

```text
ActionProposal -> ValidatedAction -> ExecutedAction
```

每个工具声明所属层、是否启用、基础影响等级、参数级影响、会失效的阶段和允许重跑起点。影响等级按风险排序：

```text
read_only -> retry_safe -> requires_confirmation -> requires_fork -> destructive
```

`RecoveryPolicy` 再按 `layer + ErrorKind + step + attempt` 做声明式裁决：指定可用工具、最大尝试次数、是否需要确认、是否只能 fork，以及从哪一步重跑。模型可以提出候选，但无法自行放宽策略。

EQ 失败是该机制的代表：模型只生成脱敏修复方案；用户确认前配置、MDP、阶段许可和进程均不改变；确认后状态必须按 `awaiting_confirmation -> retrying -> running` 流转。替代方案会产生新的 `action_id` 并使旧方案失效。

## 7. 状态机、停止与恢复

系统公开八个运行状态：`idle`、`running`、`retrying`、`awaiting_confirmation`、`stopping`、`escalated`、`done`、`aborted`。

状态机维护合法迁移表：`awaiting_confirmation` 不能直接跳到 `running`，而必须先进入 `retrying`；`stopping` 只能结束为 `aborted`；`done` 与 `aborted` 是终态。每次写入带单调 `state_revision`，通过比较后写入避免旧页面、双击确认或并发控制覆盖新状态。

状态快照只包含当前步骤、层、受限活动描述、重试次数、已应用白名单参数差异、错误摘要和完成步骤。`activity` 固定为工具、操作、对象类型、对象、当前数和总数，例如“正在使用 GROMACS 进行 NPT 退火（2/2）”。

外部命令采用受管进程组，停止时按 `SIGINT -> SIGTERM -> SIGKILL` 逐级升级并记录审计。用户文本中的“暂停”“稍后”没有停止权限。注册表可对异常退出或旧快照做受限对账，防止仍在 EQ 的任务被公开误报为 PROD。

## 8. Manifest、溯源和决策追踪

每个 `md_run/<run_id>/` 不只保存轨迹，还保存支撑复现、恢复和审计的受控记录：

| 文件 | 作用 |
|---|---|
| `config.json` | 冻结的运行配置快照 |
| `manifest.json` | run 元数据、后端、登记产物与公共审计索引 |
| `topology_manifest.json` | 拓扑组件、力场族、产物和参数化重试账本 |
| `md_manifest.json` | EM/EQ/PROD 许可、输入指纹、下游失效与盒子尝试 |
| `provenance.json` | Git 修订、运行时、配置修订、输入哈希、随机种子、能力、模型和 Prompt 版本 |
| `status.json` / `events.jsonl` | 最新公开状态与单调事件流 |
| `decision_trace.jsonl` | 脱敏的 Agent 决策轨迹 |
| `logs/structured.jsonl` | 运行期间持续追加的结构化状态、步骤、决策和进程事件；共享 run 序号且脱敏 |
| `.run-transaction.json` | 未完成关联写入的私有写前日志，恢复后删除 |
| `process_lifecycle.jsonl` | 停止或超时的受管进程摘要 |
| `mdrun_eta.json` | 从 GROMACS `mdrun -v` 解析的 ETA 快照 |

`RunStore` 对同一 run 加锁，并为 JSON 替换和 JSONL 追加的关联写入先落写前日志。进程中途退出后，下一次访问会幂等重放缺失写入，避免出现“状态已更新但事件丢失”或重放重复事件。

`logs/structured.jsonl` 由状态机、编排器、受管外部进程和 GROMACS `mdrun` 写入。步骤开始/结果、状态变化、Agent 决策、进程开始/结束及低频心跳均在对应 `md_run/<run_id>/` 内追加；它不保存命令、路径、原始输出、Prompt 或凭据，且日志写入失败不阻断计算。

决策追踪记录 `decision_id`、`action_id`、层、步骤、错误类型、命中策略、候选/允许动作、模型、Prompt 版本、确认来源、结果和成功状态。它明确排除密钥、授权头、原始 Prompt、完整日志、命令行、绝对路径和未经脱敏参数，因此可以回答“为什么重跑、谁确认、改了什么、结果如何”，又不会把运行目录变成敏感信息仓库。

## 9. 规范化机制

| 常见问题 | 统一机制 | 工程价值 |
|---|---|---|
| 步骤、名称和产物漂移 | `STEP_REGISTRY` | 编排、状态、恢复、审计和前端共享同一 10 步事实来源 |
| 外部依赖归属不清 | `EXECUTION_MODULE_REGISTRY` | 每个执行模块明确对应步骤、工具与内置依赖 |
| 配置字段无边界 | `config_schema.py` + MD 协议校验 | 拒绝错误层级，支持受控扩展和版本迁移 |
| 路径发现不一致 | `env_registry.py` / `env_checker.py` | 环境变量优先级、自动发现、预检和能力报告统一 |
| 工具越权或重试失控 | Action catalog + `RecoveryPolicy` | 风险、白名单、次数和确认要求代码化 |
| 上游修复导致下游越级 | 阶段许可 + artifact contract | 工具成功不等于步骤成功；PROD 需要真正验收的 EQ |
| 多运行目录混用 | `RunRegistry` + run-local workspace | 隔离、历史索引和受限查询 |
| 前端泄露底层细节 | 公共状态/事件与私有 manifest 分层 | 显示工序级进度，不泄露敏感实现细节 |
| 进程失控 | 受管进程组和停止升级 | checkpoint-first 停止与生命周期审计 |
| mock 被误当真实验收 | 分层 marker + external evidence | 区分单测、契约、集成、真实工具与真实 LLM 证据 |

## 10. 创新点与优势

### 10.1 受约束科学工作流，而非“LLM 调工具”

模型被放入由 schema、步骤注册表、产物许可、状态机、动作契约和恢复策略组成的工作流内核。它可以参与诊断，但不能绕过 EQ 许可、用户确认和步骤依赖。

### 10.2 化学实体 RAG 与确定性验证结合

字符级 TF-IDF 解决中文别名、离子写法和分子近似匹配；结构化工具、文件可用性和配置校验再收束结果。这比只靠 Prompt 记忆分子表更可维护，也比直接向模型投喂非结构化文档更稳定。

### 10.3 “产物即契约”

完成标准不是退出码为零，而是可消费产物、输入指纹和阶段许可。它直接防止拓扑文件缺失、EQ 未完成却提前 PROD、上游参数变更后仍消费旧 checkpoint 等跨层错误。

### 10.4 人工确认成为工作流状态

EQ 修复不是聊天建议，而是带 `action_id`、参数差异、受限重跑点和配置指纹的待确认动作。无回复保持等待，替代方案失效旧动作，确认才执行，构成可审计的人类监督闭环。

### 10.5 长任务的可观测性与可恢复性

run-local 状态、事件、写前日志、provenance、决策追踪和进程生命周期使长任务在浏览器刷新、服务重启或外部进程停止后仍可解释与恢复。ETA 仅解析 GROMACS 实际 `mdrun -v` 输出，不由模型猜测。

### 10.6 面向真实科研软件的证据门禁

外部工具验收要求版本化 fixture、SHA-256 校验、声明产物哈希和独立 execution evidence。预检成功、mock 成功或退出码为零都不能被误述为科学链路已验收。

## 11. 当前能力边界

当前代码和确定性测试已验证配置、编排、状态、恢复与多层契约；四条真实 profile 均已完成 10/10。当前成熟方案只承诺受支持 profile 的十步完成和最终无错误；G09 仅作未验证提示，科学体系预测、后处理/分析和离子 OPLS 不属于本版本公开能力。其他实验性组件不纳入本版本验收。

因此，Willy 当前最合适的定位是：**具有完整工程约束与可扩展架构的研究型内部 MD 自动化平台**。它显著降低跨软件工作流和排错成本，但只对已登记 profile 的十步无错误执行作出承诺，不对 G09、科学体系性质、后处理/分析或其他实验性组件作出本版本承诺。

## 12. 进一步阅读

- `README.md`：安装、运行和公开主流程。
- `docs/Willy.md`：项目结构与模块关系。
- `docs/status_api.md`：状态、事件和公开/私有边界。
- `docs/simulation_design.md`：MD 协议、阶段许可和回滚。
- `docs/run_assistant_design.md`：运行助理权限和演进计划。
- `docs/testing_strategy.md`：测试分层、external smoke 和发布门禁。
- `docs/project_gap_analysis.md`：真实外部验收、安全和产品化的未关闭事项。
