# Willy 项目缺口与风险台账

> 最后核验：2026-08-12，基于稳定发布 `v0.2.0`（`33e42f8`）。本文只记录当前可执行缺口和已分配任务；历史已解决事项列在文末，文档状态和责任人以 [`document_registry.md`](document_registry.md) 为准。

## 当前稳定基线

Willy 当前是可运行的研究型内部 MD 自动化 MVP，不是生产级科学计算平台。稳定版本回归为
`815 passed, 9 skipped`，收集 824 条 pytest；LLM mock eval 为 `18/18`，均分 `86.2/100`，测试台账共 842 条记录。四条已承诺 profile（G16/ORCA + Sobtop/LigParGen）均已完成 10/10；最新批次 `acceptance_batch_20260811174708.json` 对中性 `EC/FEC/EMC` 组合记录了 EM、7 ns EQ、2 ns PROD 和最终 `done`。

本版本成熟方案的边界是：受支持 profile 从 Step 1 严格完成 Step 10，受管进程正常退出，最终状态为 `done`，阶段产物和 manifest/status 通过校验，且无未处理错误。该结论是流程完整性结论，不是科学体系预测。科学预测、后处理/分析、SSH/Slurm 远程执行、G09 可靠全链路和离子 OPLS 仍不属于本版本可用范围。

## 当前任务配置

以下任务用于 v0.2.0 稳定版的收口和下一版准备；“非阻塞”任务不改变当前十步成熟方案声明。

| 任务 | 负责人 | 当前动作 | 完成证据 | 优先级 |
|------|------|------|------|------|
| T-01 发布环境复现 | 0 号、6 号 | 在干净 Python 环境重装依赖，锁定 Python/OS/外部工具版本，运行 `pip check` 和默认回归 | 可重放的环境报告、`pytest -q`、compileall、测试台账无漂移 | P1 |
| T-02 vendor 发行治理 | 0 号、5 号 | 补齐 Packmol、Sobtop、Open Babel 及示例数据的来源、许可证、版本和 SHA-256；不合格组件不进入发布工件 | `vendor/manifest.json` 全部条目有 `release_ready` 依据，或明确排除 | P1 |
| T-03 外部 profile 可重放 | 0 号、6 号 | 将四条已通过 profile 的脱敏批次证据接入具名 external case；在授权验收机串行重放，不纳入默认 CI | 四条 execution 证据、产物哈希、版本摘要和 JUnit | P1 |
| T-04 网关部署安全 | 7 号、0 号 | 保持本机/受控局域网边界；补做真实跨机器 TLS/upstream、密钥轮换、备份告警前置检查 | 部署检查清单和失败边界；不得把未完成能力写入公开主路径 | P1 |
| T-05 运行助理扩展 | 3 号、4 号、0 号 | 在现有状态、ETA、EQ 确认和停止基础上补通用错误指导、项目说明问答、resume/fork 和历史保留 | 每项能力有独立状态契约、mock eval 和用户可见结果 | P2 |
| T-06 真实 LLM/BYOK 证据 | 4 号、6 号、0 号 | 将真实端点连通性、浏览器 smoke 和 fake 外部执行严格分开；只保留脱敏结果 | opt-in 连接证据、浏览器同网络命名空间 JUnit/截图或明确失败记录 | P2 |
| T-07 公开错误与日志治理 | 0 号、3 号、4 号 | 统一用户文案、开发诊断、run 关联和脱敏规则，清理仅用于终端展示的零散输出 | 公共错误矩阵、结构化日志回归和前端文案测试 | P2 |
| T-08 文档与台账同步 | 5 号、0 号 | 检查 README、设计文档、测试策略、缺口台账和 registry 是否对稳定能力作出一致声明 | 文档链接/台账检查通过，无无责任人或过期基线 | P2 |

## 当前验收缺口

以下是稳定版本仍未完全关闭的事项。它们不否定四条 profile 的十步结果，但会限制复现、部署或后续版本声明。

### P1：发布和部署收口

| 编号 | 缺口 | 事实依据 | 关闭条件 |
|------|------|------|------|
| G-01 | 干净环境的依赖可复现性尚未形成发布证据 | 第二台受控 Ubuntu 验收机已证明在无 `WILLY_*` 覆盖时可经标准软件环境从 PATH 自动发现 G16、formchk 和 GROMACS；原 Packmol 需要 `GLIBC_2.34` 而宿主为 `GLIBC_2.31`。现内置 Packmol 已在 Ubuntu 20.04/glibc 2.31 重建为最大 `GLIBC_2.29`，本机与目标机 4 原子周期盒 smoke 通过；验收机项目检出尚未用新版本重跑完整任务 | 完成 T-01；保存脱敏环境报告和 `pip check`，更新验收机检出并在干净环境重跑默认门禁 |
| G-02 | vendor 来源/许可证/版本证据不完整 | Multiwfn 已有固定清单，其他内置或示例组件仍有待补证/排除项 | 完成 T-02；所有随包组件有可审计来源、许可证和哈希，或移出发布工件 |
| G-03 | 真实 profile 尚未固化为 self-hosted external case | 四条 profile 已有真实批次证据，但默认 CI 不执行高成本外部软件 | 完成 T-03；具名 case 能在授权机重放并生成脱敏 execution 证据 |
| G-04 | 网关仍是本机/受控局域网试运行 | 本机 loopback `/healthz` 已返回 200；离线 ASGI/fake-upstream 已通过，但远程验收机没有部署描述，真实跨机器 TLS、上游 smoke、备份告警和密钥轮换未验收 | 完成 T-04；否则 README 只能维持当前试运行表述 |

### P2：产品增强和治理

| 编号 | 缺口 | 事实依据 | 关闭条件 |
|------|------|------|------|
| G-05 | 真实 BYOK 和浏览器运行闭环缺少统一的可复核证据 | fake executor 浏览器 E2E 已有；第二台验收机的 DeepSeek thinking 配置可完成普通 Chat Completions，强制 `tool_choice` 被拒绝，但默认自动工具选择能返回 Willy 检查工具；GPT 中转站在修正目标机 Key 后已由开发者人工复测通过；真实浏览器端到端仍显式 opt-in | 完成 T-06；连接测试改为自动工具选择但强制校验指定工具调用，并将服务端明确拒绝强制选择归类为 `tool_call_unsupported`；继续保存连接、浏览器和执行证据，不上传 Key、Authorization、原始 prompt 或原始日志 |
| G-06 | 运行助理的通用问答、resume/fork 和历史保留未完成 | 当前稳定能力集中在状态、ETA、EQ 受限方案确认和停止 | 完成 T-05；每个动作均有权限、状态迁移和确认回归 |
| G-07 | 用户文案、结构化日志和开发诊断的目标机复测尚未完成 | 历史 Packmol 失败曾停在 `retrying` 且无 LLM 决策；本地已完成终态化：实际修复前不显示 retry 次数，运行时依赖错误归类为 `runtime_unavailable`，私有日志写入脱敏证据/决策，公共状态和最新错误气泡显示操作级原因、未执行参数调整及建议 | 由开发者在目标机触发同类失败并确认前端、`status.json`、事件和私有决策记录一致；再关闭 T-07 |
| G-08 | 文档基线和链接检查仍需独立门禁 | 部分路线图保留历史快照，容易与稳定基线混淆 | 完成 T-08；历史快照明确标注日期，现行结论只引用最新批次 |

> 2026-08-11 本地修复同步：Packmol 已增加 ABI/启动预检；运行时不兼容直接进入 `escalated`，不调用 LLM 或参数重试，公共状态包含受限升级摘要，私有证据包含脱敏决策记录。G-01/G-07 的代码与自动化部分已完成，目标机兼容二进制和人工复测仍是开放项。

## 需人工验证的缺口

自动化测试只能证明确定性契约，不能替代真实软件、真实设备、真实用户操作和部署环境的观察。下表是关闭 G-01--G-08 前必须完成的人工验证；未完成时，缺口只能标记为“代码/自动化通过，人工待验”。

| 缺口 | 开发者必须执行的人工验证 | AI 协作角色 | 通过条件与证据 |
|------|------|------|------|
| G-01 / T-01 | 开发者在干净机器或全新虚拟环境安装项目，按 README 配置依赖，实际运行预检、默认回归和最小导入 | 0 号制定清单，6 号生成报告 | 新环境无隐式旧配置；`pip check`、编译、默认测试和预检均通过；保存 Python/OS/工具版本及脱敏命令结果 |
| G-02 / T-02 | 开发者逐项核对内置二进制、示例数据和许可证，检查下载来源、版本、文件哈希及发布包内容 | 5 号维护 vendor 清单，0 号审查发布边界 | 每个随包文件都有来源/许可证/版本/哈希，或被明确排除；形成签字式 vendor 清单，不上传许可证之外的私有文件 |
| G-03 / T-03 | 开发者在授权验收机串行运行 G16+Sobtop、ORCA+Sobtop、G16+LigParGen、ORCA+LigParGen 四条真实十步 profile | 6 号提供 external case、证据 schema 和结果校验 | 每条从 Step 1 到 Step 10 完成，进程正常退出、状态为 `done`、产物契约通过；保存脱敏 execution JSON、JUnit 和产物哈希 |
| G-04 / T-04 | 开发者在第二台设备或受控局域网验证网关注册、批准、令牌刷新、撤销、TLS、上游失败和管理员控制面 | 7 号提供部署脚本和安全检查清单，0 号检查边界 | 非 loopback 必须 TLS；撤销后不能调用；管理员密钥不进入日志、浏览器持久存储或响应；保存部署检查单和失败边界证据 |
| G-05 / T-06 | 开发者使用真实 OpenAI-compatible/BYOK 端点完成一次连接测试、方案生成和浏览器确认流程，并验证浏览器与后端处于同一网络命名空间 | 4 号提供 UI 检查点，6 号提供脱敏证据模板 | 连接、工具调用、等待、确认、停止和工程切换均符合预期；只保存脱敏结果、截图或 JUnit，不保存 Key、Authorization、prompt 或原始响应 |
| G-06 / T-05 | 开发者人为制造 EQ 错误，分别测试查询状态、等待确认、修改方案、确认重跑、暂停文本和实际中止 | 3 号提供状态/进程观测工具，4 号提供气泡与控件检查点 | 未确认不执行；修改方案产生新 action；确认后从许可步骤重跑；暂停文本不杀进程；中止按钮能结束受管进程；保存状态/气泡顺序记录 |
| G-07 / T-07 | 开发者逐个触发输入错误、外部依赖缺失、grompp/EQ/PROD 失败和网关错误，观察用户文案与开发诊断是否分离 | 0 号维护错误分类，3/4/7 号提供各层错误样例和日志校验 | 前端只显示文件/操作级摘要和解决建议；私有日志含结构化 `run_id/step/error_kind`；无底层密钥、完整命令或原始日志泄露 |
| G-08 / T-08 | 开发者从 README 走一遍安装/运行说明，逐条核对设计文档、测试基线、缺口台账和 registry | 5 号生成链接/一致性检查报告，0 号裁决公开承诺 | 新读者路径可被复现；文档链接有效、历史快照有日期、无互相矛盾的“已完成”声明；保存审核清单 |

### 人工与 AI 边界

- **开发者是唯一真人**：只有开发者可以提供私有凭据、操作真实许可证软件、跨设备部署、观察真实 UI/进程并签署人工验收。
- **0-7 号全部是 AI 协作角色**：他们可以生成测试步骤、调用受限工具、检查证据和提出“不通过”结论，但不能代替开发者签署真实环境通过。
- **6 号、5 号、7 号不是人工测试人员或管理员**：6 号负责测试框架，5 号负责文档一致性，7 号负责网关代码与部署约束；这些职责均由 AI 执行，真人操作由开发者完成。
- **普通用户不是项目验收角色**：用户行为可作为开发者人工测试场景，不能被当作第二个真人成员或授权来源。

人工验收记录至少包含：版本/commit、测试角色、日期、环境摘要、操作结果、公开错误摘要和脱敏证据路径。不得记录 API Key、管理员密钥、Authorization、完整 prompt、原始日志或私有运行目录。

### 不列为当前缺口的事项

- 四条 G16/ORCA + Sobtop/LigParGen profile 的十步完整性：已关闭，证据见 `acceptance_batch_20260811174708.json`。
- `0.15e` 单一 Ewald 净电荷 warning 容差：已实现并回归；超过阈值、不可解析或伴随其他 warning 仍阻断。
- G09、科学体系预测、后处理/分析、SSH/Slurm、离子 OPLS 和其他未声明力场混用：明确非目标，不纳入当前版本验收。
- 网关离线协议、设备令牌、撤销、额度和管理员控制面：已有离线验收；真实跨机器部署仍属于 G-04。

## 使用规则

- 每个未解决问题必须有责任领域、事实依据、完成证据和关闭条件。
- 修复问题时，同一变更更新本台账、责任文档和测试；不能仅删除问题描述。
- 不在本文记录真实密钥、私有运行数据或未脱敏日志。安全风险只记录处理要求和验收证据。

## 本版本明确非目标

- **G09**：代码保留后端接口，但未获得授权进行可靠全链路验证。方案助理欢迎气泡必须提示“G09 未经可靠全链路验证，使用时可能出现运行问题”；不把 G09 计入本版本通过 profile。
- **科学体系预测**：本版本只验收流程是否完整执行和最终是否无错误，不对密度、结构、收敛质量或目标体系科学结论背书。
- **后处理/分析**：后处理代码和设计可保留为内部实验能力，但不列入本版本公开可用范围和发布门禁。
- **SSH/Slurm**：远程 GROMACS 不加入本版本；远程页面保持冻结并显示暂不支持，相关设计仅作为后续版本参考。
- **离子 OPLS 与其他力场混用**：继续受控拒绝，不作为本版本支持能力。

## 已关闭或已替代的历史项

| 原问题 | 当前结论 |
|------|------|
| Gradio 默认绑定 `0.0.0.0` | 已关闭：默认绑定为 `127.0.0.1`。 |
| `frontend_api.py` 使用 `os.system` 清理 | 已关闭：改为受控子进程与路径操作。 |
| `src/willy/` 中存在 5 处 `shell=True` | 已关闭：2026-08-02 全仓复核未发现 `shell = True`。后续子进程安全审计纳入 G-04/T-04 的部署与网关治理。 |
| 模拟层缺少设计文档 | 已关闭：`simulation_design.md` 已合并包含 PROD 后处理设计。 |
| 模拟层跳过分子工具缺失 | 已替代：该能力会破坏拓扑与建盒一致性，因此不对 Simulation Agent 公开。 |
| 设计文档普遍使用旧文件名 | 已关闭：当前设计文档已使用实际模块名；历史迁移名称只保留在命名与重构记录中。 |
| 缺少 CI 与测试 extras | 已关闭：默认 regression、compileall、mock eval 和测试台账质量门禁已进入 CI；外部 profile 的 self-hosted 重放仍由 G-03/T-03 跟踪。 |
| 缺少完整 `config.json` schema、唯一 step registry 与规范化依赖归属 | 已关闭：`config_schema.py` 负责外层结构；`simulation.protocol` 负责 MD v2 与迁移；`STEP_REGISTRY`/`EXECUTION_MODULE_REGISTRY` 统一步骤和依赖归属，并有契约回归。 |
| 新 run 隐式继承旧 `done_steps`，并使上游修复被误记为下游成功 | 已关闭：新 run 从 Step 1 开始；续跑必须显式提供原运行目录。Step 2 校验 `.fchk`/`.mol2` 契约，Step 3 拒绝不完整分子集合，防止错误延迟至 Sobtop。 |
| EQ 重试期间，上游修复工具成功被重标为 Step 9 成功，导致 PROD 越级启动 | 已关闭：LayerAgent 保留工具实际步骤身份；编排器按失效阶段回滚或重跑，Step 8--10 在 `mark_done` 前强制校验私有 manifest 许可和非空产物；RunRegistry 仅对历史状态越级做无损公开状态对账。回归覆盖 MDP/EM 上游修复、EQ 许可缺失和活跃 EQ 状态纠正。 |
| 方案确认与启动回执合并为重复助手气泡 | 已关闭：待确认方案后的文本启动回复记录为显式用户动作；启动回执不再重复方案摘要，并有前端与配置 Agent 回归测试。 |

## 发布前门禁

发布前，0 号、5 号和 6 号必须确认：高优先级项无未认领风险；相关外部 smoke 有证据；`pytest -q` 与 LLM mock eval 全绿；以及 `document_registry.md` 中没有无责任人的 `待同步` 文档。
